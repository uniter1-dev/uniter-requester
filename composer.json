{
    "name": "uniter1/uniter-requester",
    "description": "Uniter1 integration package. Assistant for automatic test generation. Commands for registration user and for sending yours class to generate test for it. Class for converting all methods to public on load",
    "keywords": [
        "uniter1",
        "uniter-requester"
    ],
    "homepage": "https://github.com/uniter1/uniter-requester.git",
    "license": "MIT",
    "type": "library",
    "authors": [
        {
            "name": "Eduard Fomin",
            "email": "eduard.fomin@uniter1.tech",
            "role": "Developer"
        }
    ],
    "require": {
        "php": "^7.4|^8.0",
        "ext-json": "*",
        "guzzlehttp/guzzle": "^7.4"
    },
    "require-dev": {
        "mockery/mockery": "^1.0",
        "phpunit/phpunit": "^8.0"
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true
    },
    "autoload": {
        "psr-4": {
            "Uniter1\\UniterRequester\\": "src"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Uniter1\\UniterRequester\\Tests\\": "tests"
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
